#!/usr/bin/python

class Course:
    def __init__(self, name, lectures):
        self.name = name
        #  print(self.name)
        self.lectures = lectures
        #  [print(lectures[i]) for i in range(len(lectures))]
